# demo 不调用模型，使用哈希向量和证据摘录。ollama 为真实模型模式。
APP_MODE=demo
DATA_DIR=data
STORAGE_DIR=.storage
CHUNK_SIZE=400
CHUNK_OVERLAP=60
TOP_K=4
RETRIEVAL=hybrid
OLLAMA_URL=http://localhost:11434
LLM_MODEL=qwen2.5:7b
EMBED_MODEL=nomic-embed-text
# 可选；首次启用需要下载模型，安装 uv sync --extra rerank
RERANK_MODEL=
